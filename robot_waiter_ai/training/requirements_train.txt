# Training-only dependencies — install in Colab or training venv only.
# Do NOT install these locally (they pull in heavy CUDA deps).
#
# Colab: torch, transformers, peft, bitsandbytes zaten kurulu.
# Sadece eksik olanları kur:
#   !pip install -q datasets
#
# (trl artık gerekmiyor — training/train_wbot_v1.py transformers.Trainer kullanıyor)

datasets>=2.20.0
