# Training-only dependencies — install in Colab or training venv only.
# Do NOT install these locally (they pull in heavy CUDA deps).
#
# Colab quick install:
#   !pip install -q -r robot_waiter_ai/training/requirements_train.txt
#
# (trl artık gerekmiyor — train_wbot_v2.py transformers.Trainer kullanıyor)

transformers>=4.43.0   # Qwen3 chat template desteği için zorunlu
datasets>=2.20.0
bitsandbytes>=0.46.1
peft>=0.10.0
accelerate>=0.27.0
